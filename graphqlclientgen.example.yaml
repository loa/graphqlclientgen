---
# skipGofmt: false
# createSchemaYaml: false

schema:
  - graph/*.graphqls

client:
  dir: .
  package: client

typeMappings:
  UUID:
    # alias: foobar
    name: UUID
    import: github.com/google/uuid